import React from 'react';

const Date = () => {

    const dateNow = Date;
    console.log(dateNow);
    // Date.name

    return (
        <div>
            Date
        </div>
    );
}

export default Date;
